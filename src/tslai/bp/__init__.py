"""DEFERRED: loopy belief propagation / expectation propagation over the full ARG
coupling graph (CLAUDE.md §7).

Empty until the §7.3 decision criterion triggers — i.e. only if breakpoint flicker
measured on blocked-EM output (Rung 8) is large relative to true-tract
discontinuity AND it materially degrades tract-boundary accuracy or near-boundary
calibration. Otherwise blocked EM is the published method and this stays a footnote.
"""
